import { StyleSheet } from 'react-native';
import { getCurrentAppState } from 'react-native-mock-render/build/NativeModules/AppState';

// Style sheet for grocery list

export const styles = StyleSheet.create({

    name: {
        fontSize: 20,
        color: 'black',
        justifyContent: 'center', 
        alignItems: 'center', 
        
    },

})